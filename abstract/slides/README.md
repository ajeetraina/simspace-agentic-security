# slides

When we say the agentic stack, we mean your container foundation ~ the images an agent pulls dependencies into, builds from, and invokes as tools. That’s the stack. This session secures all three.
